Update RoleChips to hide role buttons for other admin users
Update UserActions to hide all action buttons for other admin users
Ensure `me` is available (use `meLoading`) to avoid flicker
Add tests verifying hidden behavior for other-admin rows
Build, test, and commit with message: "users: hide role/action buttons for other admins; hide instead of disable"
